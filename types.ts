
export interface KeywordData {
  keyword: string;
  searchVolume: number;
  competition: number; // A score from 1 (Low) to 100 (High)
}

export interface ContentIdeas {
    titles: string[];
    description: string;
}
